{
  "description": "Parse JSON from the log message field (e.g. S3 server access log structured message). Stores parsed object under s3.parsed; non-JSON messages are left unchanged.",
  "processors": [
    {
      "json": {
        "field": "message",
        "target_field": "s3.parsed",
        "ignore_failure": true
      }
    }
  ]
}
