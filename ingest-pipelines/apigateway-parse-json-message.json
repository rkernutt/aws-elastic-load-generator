{
  "description": "Parse JSON from the log message field (e.g. API Gateway access logs). Stores parsed object under apigateway.parsed; non-JSON messages are left unchanged.",
  "processors": [
    {
      "json": {
        "field": "message",
        "target_field": "apigateway.parsed",
        "ignore_failure": true
      }
    }
  ]
}
