{
  "description": "Parse JSON from the log message field (e.g. AWS Glue continuous logging). Stores parsed object under glue.parsed; non-JSON messages are left unchanged.",
  "processors": [
    {
      "json": {
        "field": "message",
        "target_field": "glue.parsed",
        "ignore_failure": true
      }
    }
  ]
}
