{
  "description": "Parse JSON from the log message field (e.g. RDS enhanced monitoring). Stores parsed object under rds.parsed; non-JSON messages are left unchanged.",
  "processors": [
    {
      "json": {
        "field": "message",
        "target_field": "rds.parsed",
        "ignore_failure": true
      }
    }
  ]
}
