{
  "description": "Parse JSON from the log message field (e.g. SageMaker Studio continuous logging). Stores parsed object under sagemaker.parsed; non-JSON messages are left unchanged.",
  "processors": [
    {
      "json": {
        "field": "message",
        "target_field": "sagemaker.parsed",
        "ignore_failure": true
      }
    }
  ]
}
