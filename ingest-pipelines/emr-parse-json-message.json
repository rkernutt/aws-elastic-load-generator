{
  "description": "Parse JSON from the log message field (e.g. EMR structured logging). Stores parsed object under emr.parsed; non-JSON messages are left unchanged.",
  "processors": [
    {
      "json": {
        "field": "message",
        "target_field": "emr.parsed",
        "ignore_failure": true
      }
    }
  ]
}
