{
  "description": "Parse JSON from the log message field (e.g. ECS container structured logs). Stores parsed object under ecs.parsed; non-JSON messages are left unchanged.",
  "processors": [
    {
      "json": {
        "field": "message",
        "target_field": "ecs.parsed",
        "ignore_failure": true
      }
    }
  ]
}
